// Test-stub: import map orqali esm.sh o'rniga yuklanadi (tarmoqsiz shim-sinov).
// Ishlab chiqarishда ISHLATILMAYDI — faqat migration/shim-selftest.html uchun.
export function createClient() { return {}; }
